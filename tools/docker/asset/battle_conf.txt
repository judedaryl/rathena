

// Rate at which exp. is given. (Note 2)
base_exp_rate: 10000

// Rate at which job exp. is given. (Note 2)
job_exp_rate: 10000
// Turn this on to allow a player to level up more than once from a kill. (Note 1)
multi_level_up: yes

// The rate the common items are dropped (Items that are in the ETC tab, besides card)
item_rate_common: 5000
item_rate_common_boss: 5000
item_rate_common_mvp: 5000
item_drop_common_min: 50
item_drop_common_max: 500000


// The rate healing items are dropped (items that restore HP or SP)
item_rate_heal: 5000
item_rate_heal_boss: 5000
item_rate_heal_mvp: 5000
item_drop_heal_min: 50
item_drop_heal_max: 500000

// The rate at which usable items (in the item tab) other then healing items are dropped.
item_rate_use: 5000
item_rate_use_boss: 5000
item_rate_use_mvp: 5000
item_drop_use_min: 50
item_drop_use_max: 500000

// The rate at which equipment is dropped.
item_rate_equip: 5000
item_rate_equip_boss: 5000
item_rate_equip_mvp: 5000
item_drop_equip_min: 50
item_drop_equip_max: 500000

// The rate at which cards are dropped
item_rate_card: 5000
item_rate_card_boss: 5000
item_rate_card_mvp: 5000
item_drop_card_min: 50
item_drop_card_max: 500000

// Minimum allowed delay for ANY skills after castbegin (in milliseconds) (Note 1)
// Note: Setting this to anything above 0 can stop speedhacks.
min_skill_delay_limit: 0


// Delay time after casting (Note 2)
delay_rate: 100